export enum OrderActions {
  select_chain_base = 'select_chain_base',
  select_buy_order = 'select_buy_order',
  select_sell_order = 'select_sell_order',
}

export enum OrderActionsName {
  select_chain_base = 'Base',
  select_buy_order = 'Buy',
  select_sell_order = 'Sell',
}
